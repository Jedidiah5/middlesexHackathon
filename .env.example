# Copy to .env.local and fill in values (never commit real keys).
#
# Dummy Claude replies (no API bill): leave ANTHROPIC_API_KEY unset, OR set CLAUDE_USE_DUMMY=1.
# Live Claude: set ANTHROPIC_API_KEY and CLAUDE_USE_DUMMY=0 (or unset CLAUDE_USE_DUMMY).
ANTHROPIC_API_KEY=
CLAUDE_USE_DUMMY=
